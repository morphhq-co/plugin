{
  "_comment": "Reference template — copy the 'hooks' object into your .qoder/settings.json or ~/.qoder/settings.json and replace MORPH_PLUGIN_DIR with the path to your checkout of this repo. Qoder accepts the hookSpecificOutput.additionalContext shape, which is the default output.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node MORPH_PLUGIN_DIR/hooks/morph-brain-hook.js user-prompt-submit"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node MORPH_PLUGIN_DIR/hooks/morph-brain-hook.js pre-tool-use"
          }
        ]
      }
    ]
  }
}
