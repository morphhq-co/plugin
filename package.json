{
  "name": "@morphhq/plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Morph agent plugin: the Morph MCP server plus lifecycle hooks that pull shared organization guidance from brain/hooks/*.md.",
  "repository": "github:morphhq-co/plugin",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test"
  }
}
